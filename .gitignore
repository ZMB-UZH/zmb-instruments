# Local raw data — never commit
# Raw CSV exports from instrument software stay local, not on GitHub
**/raw/

# Internal staff SOPs / local-only notes — contain internal Z: paths, never publish
**/STAFF_UPDATE.md
*.local.md
# Internal handover / session history (Magnolia/Dozuki/internal paths) — never publish
/_handovers/

# QC run sidecar (measurement date written by update_data.py, read by bat)
**/.qc_last_date

# QC update log (local audit trail, never published)
/logs/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*~
